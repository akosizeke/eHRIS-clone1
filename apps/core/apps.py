from django.apps import AppConfig


# Django app configuration for the core dashboard module.
class CoreConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.core'
